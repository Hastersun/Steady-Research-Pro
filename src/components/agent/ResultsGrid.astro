---
const sections = [
  { key: 'resultsGrid.sectionFramework', fallback: '模型评测框架' },
  { key: 'resultsGrid.sectionBenchmark', fallback: 'Benchmark 对比' },
  { key: 'resultsGrid.sectionExperiment', fallback: '实验设计建议' },
  { key: 'resultsGrid.sectionRisk', fallback: '风险与限制' },
  { key: 'resultsGrid.sectionTrend', fallback: '趋势洞察' },
  { key: 'resultsGrid.sectionAction', fallback: '行动清单' }
]
const loading = false // 后续可由父级状态传入
---
<div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3">
  {loading && Array.from({length:6}).map((_,i)=>(
    <div class="relative overflow-hidden rounded-2xl border border-white/10 bg-slate-900/55 p-5 shadow-soft animate-fade-in" style={`animation-delay:${i*60}ms`}>
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(59,130,246,0.2),_transparent_70%)] opacity-40"></div>
      <div class="relative h-3 w-24 rounded bg-white/10 mb-4"></div>
      <div class="space-y-2 mb-4 relative">
        {Array.from({length:4}).map((__,j)=>(
          <div class="h-2.5 rounded bg-white/10 w-full overflow-hidden relative">
            <span class="absolute inset-0 -translate-x-full bg-gradient-to-r from-transparent via-white/70 to-transparent animate-shimmer"></span>
          </div>
        ))}
      </div>
      <div class="relative flex items-center justify-between">
        <div class="h-2 w-20 rounded bg-white/10"></div>
        <div class="h-6 w-14 rounded bg-white/10"></div>
      </div>
    </div>
  ))}
  {!loading && sections.map((title, i) => (
    <article class="group relative overflow-hidden rounded-2xl border border-white/10 bg-slate-900/55 backdrop-blur p-6 shadow-soft transition-all duration-300 hover:-translate-y-1 hover:border-brand-400/40 hover:shadow-xl animate-slide-up" style={`animation-delay:${i*70}ms`}>
      <div class="absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100" style="background: radial-gradient(circle at top, rgba(45, 212, 191, 0.22), transparent 60%);"></div>
      <div class="relative flex items-center justify-between">
        <div class="flex items-center gap-2">
          <span class="inline-flex h-7 w-7 items-center justify-center rounded-full border border-white/10 bg-white/10 text-[11px] font-semibold text-white/80">{String(i + 1).padStart(2, '0')}</span>
          <h3 class="text-sm font-semibold text-white flex items-center gap-2">
            <svg class="w-4 h-4 text-brand-200" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6l4 2"/></svg>
            <span data-i18n={title.key}>{title.fallback}</span>
          </h3>
        </div>
        <span class="text-[11px] uppercase tracking-[0.22em] text-slate-400/70" data-i18n="resultsGrid.moduleLabel">Module</span>
      </div>
      <p class="relative mt-3 text-xs text-slate-200/80 line-clamp-5 leading-relaxed" data-i18n="resultsGrid.placeholderDescription">
        这是一个占位摘要。后续可动态替换为模型输出内容，支持富文本、引用与高亮。
      </p>
      <div class="relative mt-4 flex items-center justify-between text-[11px] text-slate-300/80">
        <span>
          <span data-i18n="resultsGrid.confidenceLabel">置信度</span>
          <span>: </span>
          <span class="text-brand-200 font-semibold">0.82</span>
        </span>
        <button class="inline-flex items-center gap-1 rounded-full border border-white/10 bg-white/10 px-3 py-1 text-slate-200/80 transition hover:border-brand-400/40 hover:bg-brand-400/10 hover:text-white">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 20l9-16H3l9 16z"/></svg>
          <span data-i18n="resultsGrid.expand">展开详情</span>
        </button>
      </div>
    </article>
  ))}
</div>